# 랭턴의 개미 (Langton's Ant) — 2규칙 튜링 기계

> **[AI Implementation Context]**
> 이 문서는 해당 알고리즘을 다른 프로그래밍 언어(C++, Python, Unity, Rust 등)나 그래픽스 환경으로 이식(Porting)할 때 참조하기 위한 기하학/알고리즘 명세서입니다. 특정 언어의 그래픽스 API에 종속되지 않은 순수 논리와 수학적 규칙을 담고 있습니다.


## 1. 개요 및 목적 (Overview)
격자 위 개미가 단 두 규칙으로 움직이며 칸 색을 뒤집는다. 처음 ~1만 스텝은 혼돈처럼 보이다가, 갑자기 규칙적인 '고속도로' 패턴으로 질서가 창발한다.

## 2. 핵심 이론 및 원리 (Core Concept & Math)
```
현재 칸이 흰색이면:  오른쪽으로 90° 회전, 칸을 검게, 한 칸 전진
현재 칸이 검은색이면: 왼쪽으로 90° 회전, 칸을 희게, 한 칸 전진
```
회전은 방향벡터 `(dx,dy)`의 90° 변환:
- 우회전: `(dx,dy) → (−dy, dx)`
- 좌회전: `(dx,dy) → (dy, −dx)`

## 3. 알고리즘 의사코드 (Pseudocode)
```
개미 위치·방향 초기화
repeat 스텝:
    if grid[pos]==0: 우회전; grid[pos]=1
    else:            좌회전; grid[pos]=0
    (그 칸 다시 그림)
    pos += 방향 (토러스 wrap)
```

### 주요 파라미터 (Main Parameters)
- 격자 250², 프레임당 300스텝. 약 10,000스텝 후 '고속도로' 출현.

## 4. 데이터 구조 및 이식 가이드 (Data Structures & Porting Guide)
- 배열 + 방향벡터 회전이면 끝. 매우 이식성 높음.
- 다색·다규칙(Turmite)으로 확장하면 다양한 창발 패턴.
- 이 개미는 튜링 완전 — 단순 규칙의 계산 보편성 예시.
